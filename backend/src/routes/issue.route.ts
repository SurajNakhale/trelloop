import { Router } from "express";


const issueRoutes = Router();

issueRoutes.post("/", );
issueRoutes.get("/", );
issueRoutes.patch("/:issueId", );
issueRoutes.delete("/:issueId", )

export default issueRoutes