import { Router } from "express";


const boardRoutes = Router();

boardRoutes.post("/", );
boardRoutes.get("/", );
boardRoutes.get("/:boardId", );
boardRoutes.patch("/:boardId", );
boardRoutes.delete("/:boardId", );

export default boardRoutes;
